<div class="row">
	<div class="col-sm-12">
		<%if current_user.has_role?('author') || current_user.has_role?('admin')%>
			<div class="col-sm-3">
				<a href="/articles/new" class="btn btn-primary">New Blog</a>
			</div>
		<%end%>	
	</div>
	<div class="col-sm-12">
		<%= render 'articles', articles: @articles %>
	</div>
</div>